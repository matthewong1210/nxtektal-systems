# Local-only V0 broker. Compose publishes this listener on host loopback only.
# Do not run this all-interface listener natively; use the documented temporary
# loopback-bound derivative instead.
listener 1883 0.0.0.0
allow_anonymous true
persistence false
max_packet_size 65536
log_dest stdout
connection_messages true
