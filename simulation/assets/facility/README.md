# Facility assets (placeholder slot)

Empty by design.

Will hold the ~10 m x 10 m handoff/docking zone stage for Isaac Sim:

* ground plane with configurable slope (matches
  `scenario.facility.ground_slope_deg`)
* station mounting pad + charge dock marker
* simple obstacle props for e-stop scenarios

This is NOT a golf-course digital twin; keep it minimal and functional.
