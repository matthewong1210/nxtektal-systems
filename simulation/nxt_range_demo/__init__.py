"""NXTektal Range Operations Demo Viewer (Streamlit).

Read-only presentation layer over a demo bundle exported by
``nxt_range_viewer`` (layout.json / episode.json / benchmark.json). No
simulator imports, no backend, no persistence — everything on screen comes
from the bundle files, and every number is a SIMULATION RESULT from
placeholder-provenance parameters.
"""

DEMO_VERSION = "0.1.0"
